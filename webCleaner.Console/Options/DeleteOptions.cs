namespace webCleaner.Options
{
    public enum DeleteOption
    {
        TemporaryInternetFiles,
        Cookies,
        History,
        DownloadHistory,
        FormData,
        Passwords,
        Cache,
        ActiveLogins
    }
}

